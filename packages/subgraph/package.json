{
  "name": "@scaffold-eth/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:staging": "graph deploy supriyaamisshra/tokenstreams --product hosted-service --access-token <ACCESS_TOKEN> --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/",
    "deploy:goerli": "graph deploy supriyaamisshra/tokenstreams-goerli --product hosted-service --access-token <ACCESS_TOKEN> --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/",
    "create-local": "graph create --node http://localhost:8020/ tokenstreams/org-factory",
    "remove-local": "graph remove --node http://localhost:8020/ tokenstreams/org-factory",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tokenstreams/org-factory"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  },
  "devDependencies": {
    "mustache": "^3.1.0"
  }
}
